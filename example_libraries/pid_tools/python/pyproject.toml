[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pid_tools"
version = "1.3.0"
description = "Example PID helpers for the SystemCore blocks interface"
requires-python = ">=3.8"
license = {text = "Apache-2.0"}
dependencies = []

[tool.setuptools]
packages = ["pid_tools"]
