[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "led_effects"
version = "2.1.0"
description = "Example LED effect helpers for the SystemCore blocks interface"
requires-python = ">=3.8"
license = {text = "Apache-2.0"}
dependencies = []

[tool.setuptools]
packages = ["led_effects"]
