[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "blocks_demo"
version = "1.0.0"
description = "Demo third party library for the SystemCore blocks interface"
requires-python = ">=3.8"
license = {text = "Apache-2.0"}
dependencies = []

[tool.setuptools]
packages = ["blocks_demo"]
